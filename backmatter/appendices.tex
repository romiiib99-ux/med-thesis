\newpage
\begin{center}
{\XTitr\fontsize{20pt}{24pt}\selectfont فرم جمع‌آوری اطلاعات پژوهشی}
\end{center}

\noindent کد شناسایی بیمار: .................... \hfill تاریخ مصاحبه: ....................

\medskip
با سلام و احترام،

این فرم جهت انجام یک طرح تحقیقاتی پزشکی تهیه شده است. اطلاعات شما کاملاً محرمانه باقی خواهد ماند و نیازی به نوشتن نام و نام خانوادگی نیست. لطفاً با دقت و صداقت به سوالات پاسخ دهید.

\subsection*{بخش اول: اطلاعات دموگرافیک و اجتماعی}

\noindent 1. \textbf{سن:} ............ سال

\noindent 2. \textbf{جنسیت:} \checkbox{} مرد \quad \checkbox{} زن

\noindent 3. \textbf{وضعیت تأهل:} \checkbox{} مجرد \quad \checkbox{} متأهل \quad \checkbox{} طلاق‌گرفته/همسر فوت شده

\noindent 4. \textbf{سطح تحصیلات:} \checkbox{} بی‌سواد/ابتدایی/سیکل/دیپلم \quad \checkbox{} دانشگاهی (بالاتر از دیپلم)

\noindent 5. \textbf{وضعیت شغلی:} \checkbox{} شاغل \quad \checkbox{} بیکار/بازنشسته/خانه‌دار

\noindent 6. \textbf{محل سکونت:} \checkbox{} شهری \quad \checkbox{} روستایی

\noindent 7. \textbf{وضعیت اقتصادی (بر اساس ارزیابی بیمار از توانایی تأمین مخارج):}

\noindent \checkbox{} خوب (راحت هستم) \quad \checkbox{} متوسط (دخل و خرج برابر است) \quad \checkbox{} ضعیف (با مشکلات مالی روبرو هستم)

\noindent 8. \textbf{وضعیت پوشش بیمه درمانی:} \checkbox{} دارم \quad \checkbox{} ندارم

\subsection*{بخش دوم: سوابق بالینی}

\noindent 9. \textbf{زمان سپری‌شده از تشخیص سکته قلبی (\lr{MI}):} ............ روز

\noindent 10. \textbf{نوع اقدام درمانی انجام شده برای رویداد اخیر:}
\begin{itemize}[label={}]
  \item \checkbox{} آنژیوپلاستی (فنر گذاری / استنت)
  \item \checkbox{} جراحی بای‌پس عروق کرونری (عمل باز قلب)
  \item \checkbox{} درمان دارویی (بدون مداخله جراحی)
\end{itemize}

\noindent 11. \textbf{ابتلا به سایر بیماری‌ها (همه موارد را علامت بزنید):}
\begin{itemize}[label={}]
  \item \checkbox{} فشار خون بالا
  \item \checkbox{} نارسایی قلبی
  \item \checkbox{} بیماری ریوی
  \item \checkbox{} دیابت (قند خون)
  \item \checkbox{} چربی خون بالا
  \item \checkbox{} نارسایی کلیه
  \item \checkbox{} هیچکدام
\end{itemize}

\noindent اگر بیماری دارید که در لیست بالا ذکر نشده است، آن را بنویسید: ......................................

\noindent 12. \textbf{سابقه خانوادگی بیماری قلبی زودرس (پدر/برادر زیر 55 سال یا مادر/خواهر زیر 65 سال):} \checkbox{} دارم \quad \checkbox{} ندارم

\noindent 13. \textbf{وضعیت شرکت در برنامه بازتوانی قلبی:} \checkbox{} شرکت کرده‌ام \quad \checkbox{} شرکت نکرده‌ام

\subsection*{بخش سوم: عوامل فردی و روانشناختی}

\noindent 14. \textbf{چقدر در پر کردن فرم‌های پزشکی به تنهایی، به کمک دیگران نیاز دارید؟ (\lr{SILS})}

\noindent \checkbox{} اصلاً \quad \checkbox{} کمی \quad \checkbox{} تا حدودی \quad \checkbox{} زیاد \quad \checkbox{} کاملاً

\noindent 15. \textbf{باورهای شما درباره بیماری (لطفاً از 0 تا 10 نمره دهید): (\lr{Brief IPQ})}

\begin{itemize}
  \item \textbf{بیماری شما چقدر بر زندگی‌تان تأثیر گذاشته است؟}

  (0=هیچ تأثیری ندارد ... 10=تأثیر شدید بر زندگی دارد): [......]

  \item \textbf{فکر می‌کنید بیماری شما چقدر طول می‌کشد؟}

  (0=مدت خیلی کوتاه ... 10=برای همیشه باقی می‌ماند): [......]

  \item \textbf{چقدر احساس می‌کنید خودتان بر بیماری‌تان کنترل دارید؟}

  (0=هیچ کنترلی ندارم ... 10=کنترل کامل دارم): [......]

  \item \textbf{چقدر فکر می‌کنید اقدامات درمانی (دارو/سبک زندگی) بیماری شما را بهبود می‌بخشد؟}

  (0=هیچ کمکی نمی‌کند ... 10=بسیار کمک‌کننده است): [......]

  \item \textbf{چقدر علائم جسمانی شدید (مثل درد یا تنگی نفس) را تجربه می‌کنید؟}

  (0=هیچ علامتی ندارم ... 10=علائم شدید دارم): [......]

  \item \textbf{چقدر نگران بیماری قلبی خود هستید؟}

  (0=اصلاً نگران نیستم ... 10=بسیار نگرانم): [......]

  \item \textbf{چقدر بیماری خود را می‌شناسید و ماهیت آن را درک می‌کنید؟}

  (0=اصلاً نمی‌شناسم ... 10=کاملاً درک می‌کنم): [......]

  \item \textbf{بیماری شما چقدر باعث ناراحتی روحی (عصبانیت، ترس، غمگینی) شما شده است؟}

  (0=اصلاً تأثیر روحی نداشته ... 10=تأثیر روحی شدید داشته): [......]
\end{itemize}

\noindent 16. \textbf{پایبندی دارویی (\lr{MMAS-4}):}
\begin{itemize}
  \item آیا تاکنون فراموش کرده‌اید داروهای قلبی خود را مصرف کنید؟ \checkbox{} بله \quad \checkbox{} خیر
  \item آیا گاهی در مصرف دقیق داروها سر ساعت کوتاهی می‌کنید؟ \checkbox{} بله \quad \checkbox{} خیر
  \item وقتی احساس بهبودی می‌کنید، آیا مصرف دارو را قطع می‌کنید؟ \checkbox{} بله \quad \checkbox{} خیر
  \item اگر با مصرف دارو احساس بدی پیدا کنید، آیا مصرف آن را قطع می‌کنید؟ \checkbox{} بله \quad \checkbox{} خیر
\end{itemize}

\subsection*{بخش چهارم: سنجش پایبندی به اقدامات پیشگیری ثانویه}

\noindent \textbf{الف) مصرف دخانیات:}

\noindent 17. \textbf{آیا در 30 روز گذشته سیگار، قلیان یا هرگونه دخانیات مصرف کرده‌اید؟}

\noindent \checkbox{} بله \quad \checkbox{} خیر

\medskip
\noindent 18. \textbf{ب) فعالیت بدنی (\lr{IPAQ-Short})}
\begin{itemize}
  \item \textbf{در ۷ روز گذشته، چند روز فعالیت بدنی شدید (مثل دویدن یا بلند کردن وزنه سنگین) داشتید؟}

  \checkbox{} خیر، نداشتم \quad \checkbox{} بله، ..... روز در هفته

  \item \textbf{در این روزها، معمولاً چند دقیقه فعالیت شدید انجام دادید؟} ..... دقیقه

  \item \textbf{در ۷ روز گذشته، چند روز فعالیت بدنی متوسط (مثل پیاده‌روی تند یا شنای تفریحی) داشتید؟}

  \checkbox{} خیر، نداشتم \quad \checkbox{} بله، ..... روز در هفته

  \item \textbf{در این روزها، معمولاً چند دقیقه فعالیت متوسط انجام دادید؟} ..... دقیقه

  \item \textbf{در ۷ روز گذشته، چند روز پیاده‌روی (برای کار یا تفریح) داشتید؟}

  \checkbox{} خیر، نداشتم \quad \checkbox{} بله، ..... روز در هفته

  \item \textbf{در این روزها، معمولاً چند دقیقه پیاده‌روی کردید؟} ..... دقیقه

  \item \textbf{در طول یک روز معمولی، چقدر زمان صرف نشستن (مطالعه، تماشای تلویزیون و...) می‌کنید؟} ..... ساعت و ..... دقیقه
\end{itemize}

\noindent 19. \textbf{ج) تغذیه سالم (\lr{MEDAS}):}
\begin{itemize}
  \item \textbf{آیا از روغن زیتون (یا روغن مایع گیاهی) به عنوان روغن اصلی پخت‌وپز استفاده می‌کنید؟} \checkbox{} بله \quad \checkbox{} خیر
  \item \textbf{آیا روزانه ۴ قاشق غذاخوری یا بیشتر روغن زیتون/مایع مصرف می‌کنید؟} \checkbox{} بله \quad \checkbox{} خیر
  \item \textbf{آیا روزانه حداقل ۲ واحد سبزیجات (معادل ۴۰۰ گرم) مصرف می‌کنید؟} \checkbox{} بله \quad \checkbox{} خیر
  \item \textbf{آیا روزانه حداقل ۳ واحد میوه مصرف می‌کنید؟} \checkbox{} بله \quad \checkbox{} خیر
  \item \textbf{آیا مصرف گوشت قرمز (گوسفند/گوساله) یا فرآورده‌های گوشتی (سوسیس/کالباس) کمتر از ۱ بار در روز است؟} \checkbox{} بله \quad \checkbox{} خیر
  \item \textbf{آیا مصرف کره، خامه یا مارگارین کمتر از ۱ قاشق در روز است؟} \checkbox{} بله \quad \checkbox{} خیر
  \item \textbf{آیا نوشیدنی‌های شیرین (نوشابه گازدار) کمتر از ۱ بار در روز مصرف می‌کنید؟} \checkbox{} بله \quad \checkbox{} خیر
  \item \textbf{آیا در هفته ۳ بار یا بیشتر حبوبات مصرف می‌کنید؟} \checkbox{} بله \quad \checkbox{} خیر
  \item \textbf{آیا در هفته ۳ بار یا بیشتر ماهی یا غذاهای دریایی مصرف می‌کنید؟} \checkbox{} بله \quad \checkbox{} خیر
  \item \textbf{آیا شیرینی‌جات تجاری (کیک، بیسکویت، کلوچه) کمتر از ۳ بار در هفته مصرف می‌کنید؟} \checkbox{} بله \quad \checkbox{} خیر
  \item \textbf{آیا در هفته ۳ بار یا بیشتر آجیل/مغزیجات مصرف می‌کنید؟} \checkbox{} بله \quad \checkbox{} خیر
  \item \textbf{آیا ترجیحاً از گوشت سفید (مرغ/بوقلمون) به جای گوشت قرمز استفاده می‌کنید؟} \checkbox{} بله \quad \checkbox{} خیر
  \item \textbf{آیا در هفته حداقل ۲ بار خوراک‌های سنتی با سس گوجه‌فرنگی و روغن مایع (مشابه املت یا خورش‌های گیاهی) مصرف می‌کنید؟} \checkbox{} بله \quad \checkbox{} خیر
\end{itemize}

\noindent \textbf{د) وضعیت مدیریت استرس (\lr{PHQ-4}):}

\noindent 20. \textbf{در 2 هفته گذشته، چقدر به علت مشکلات زیر آزرده شدید؟}
\begin{itemize}
  \item احساس عصبی بودن، اضطراب یا تنش؟

  اصلاً \checkbox{} / چندین روز \checkbox{} / بیش از نیمی از روزها \checkbox{} / تقریباً هر روز \checkbox{}

  \item ناتوانی در توقف نگرانی یا کنترل آن؟

  اصلاً \checkbox{} / چندین روز \checkbox{} / بیش از نیمی از روزها \checkbox{} / تقریباً هر روز \checkbox{}

  \item بی‌علاقگی یا عدم لذت بردن از کارها؟

  اصلاً \checkbox{} / چندین روز \checkbox{} / بیش از نیمی از روزها \checkbox{} / تقریباً هر روز \checkbox{}

  \item احساس خلق ناراحت، افسردگی یا ناامیدی؟

  اصلاً \checkbox{} / چندین روز \checkbox{} / بیش از نیمی از روزها \checkbox{} / تقریباً هر روز \checkbox{}
\end{itemize}

\begin{center}
\textbf{با تشکر از همکاری شما}
\end{center}

